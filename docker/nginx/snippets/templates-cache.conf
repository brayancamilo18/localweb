# Incluir en el server block de nginx del front en producción (VPS).
# Ejemplo: include /var/www/onez/docker/nginx/snippets/templates-cache.conf;
location ^~ /templates/ {
    add_header Cache-Control "public, max-age=300, must-revalidate" always;
}
